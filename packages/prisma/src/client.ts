/**
 * Placeholder exports until the Prisma client is generated.
 */
export interface PrismaClientPlaceholder {
  readonly $disconnect: () => Promise<void>;
}

export const prismaClient: PrismaClientPlaceholder = {
  $disconnect: async () => Promise.resolve(),
};
